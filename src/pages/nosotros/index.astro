---
import Layout from '../../layouts/Layout.astro';
import { Image } from 'astro:assets';
import ven01 from '../../assets/ven01.webp';
import ven02 from '../../assets/ven02.webp';
import ven03 from '../../assets/ven03.webp';
import ven04 from '../../assets/ven04.webp';
import ven05 from '../../assets/ven05.webp';
import ven06 from '../../assets/ven06.webp';
import ven07 from '../../assets/ven07.webp';
---

<Layout title="Nosotros" description="Nosotors de JRC Ingenieros y Consultores">

    <section class="hero-standard animated-hero"> <!-- Reusable hero class -->
        <div class="hero-standard-overlay"></div>
        <div class="container hero-standard-content">
            <h1 class="hero-title">NOSOTROS</h1>
            <!-- Optional: Short tagline for the about us page -->
            <p class="hero-subtitle">Conectando innovación, experiencia y compromiso para construir un futuro con propósito.</p>
        </div>
    </section>

    <section id="proposito" class="section purpose-section bg-white">
        <div class="container narrow-container animate-on-scroll"> <!-- Narrow container for focus -->
            <h2 class="section-title">Nuestro Propósito</h2>
            <p class="purpose-text">
                En JRC Ingenieros y Consultores SAC, impulsamos el crecimiento de nuestros clientes con soluciones inteligentes, sostenibles y confiables. Nuestra razón de ser, es transformar ideas en proyectos exitosos, conectando innovación, experiencia y compromiso para construir un futuro con propósito.
            </p>
        </div>
    </section>

    <section id="mision-vision" class="section mission-vision-section bg-light">
        <div class="container">
            <div class="mission-vision-grid">
                <div class="mission-vision-card animate-on-scroll">
                    <h3>Misión</h3>
                    <p>Brindar servicios integrales de consultoría, ingeniería y supervisión con altos estándares de calidad, eficiencia y responsabilidad, contribuyendo al desarrollo sostenible de nuestros clientes y del entorno. Nos comprometemos a ofrecer soluciones innovadoras y personalizadas, respaldadas por un equipo técnico altamente calificado y una gestión orientada a la excelencia.</p>
                </div>
                <div class="mission-vision-card animate-on-scroll" data-aos-delay="200"> <!-- Example AOS delay -->
                    <h3>Visión</h3>
                    <p>Ser una empresa líder a nivel nacional y reconocida a nivel internacional por la calidad de nuestros servicios de consultoría, ingeniería y supervisión, destacando por nuestra ética profesional, compromiso con el desarrollo sostenible y la generación de valor para nuestros clientes y la sociedad.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="ventajas" class="section advantages-section bg-white">
        <div class="container">
            <h2 class="section-title">Nuestras Ventajas Competitivas</h2>
            <p class="advantages-intro">
                En JRC Ingenieros y Consultores S.A.C. nos destacamos por ofrecer un valor diferencial en cada uno de nuestros proyectos. Las más resaltantes son:
            </p>
            <div class="advantages-grid">
                <!-- Advantage 1 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven01} alt="Experiencia Multidisciplinaria" class="advantage-icon" /> 
                    <h4>Experiencia Multidisciplinaria</h4>
                    <p>Equipo técnico altamente calificado con amplia experiencia en diversas áreas, abordando proyectos de forma integral.</p>
                </div>
                <!-- Advantage 2 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven02} alt="Compromiso Ambiental" class="advantage-icon" /> 
                    <h4>Compromiso Ambiental</h4>
                    <p>Integramos prácticas responsables y soluciones sostenibles que generan un impacto positivo.</p>
                </div>
                <!-- Advantage 3 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven03} alt="Calidad y Cumplimiento" class="advantage-icon" /> 
                    <h4>Calidad y Cumplimiento</h4>
                    <p>Garantizamos altos estándares de calidad, cumpliendo rigurosamente con plazos y normativas.</p>
                </div>
                <!-- Advantage 4 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven04} alt="Atención Personalizada" class="advantage-icon" /> 
                    <h4>Atención Personalizada</h4>
                    <p>Entendemos a fondo las necesidades de nuestros clientes, brindando asesoría cercana y soluciones a medida.</p>
                </div>
                <!-- Advantage 5 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven05} alt="Tecnología Avanzada" class="advantage-icon" /> 
                    <h4>Tecnología Avanzada</h4>
                    <p>Aplicamos tecnologías modernas y software especializado para optimizar diseño, análisis y supervisión.</p>
                </div>
                <!-- Advantage 6 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven06} alt="Innovación Continua" class="advantage-icon" /> 
                    <h4>Innovación Continua</h4>
                    <p>Promovemos una cultura de mejora constante, manteniéndonos a la vanguardia del sector.</p>
                </div>
                 <!-- Advantage 7 -->
                <div class="advantage-card animate-on-scroll">
                    <Image src={ven07} alt="Reputación y Confianza" class="advantage-icon" /> 
                    <h4>Reputación y Confianza</h4>
                    <p>Nuestra trayectoria y clientes satisfechos avalan la calidad y seriedad de nuestros servicios.</p>
                </div>
            </div>
        </div>
    </section>

</Layout>

<style>
    /* --- Reusable Hero Style (if not already generic enough) --- */
.hero-standard {
    position: relative;
    background-color: var(--color-secondary); /* Fallback */
    /* *** Add a relevant, high-quality background image for About Us page *** */
    background-image: url('../../../public/bg-about.webp');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    color: var(--color-white);
    padding: 90px 0; /* Adjust padding */
    min-height: 30vh; /* Adjust height */
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    overflow: hidden;
}
.hero-standard-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(47, 62, 70, 0.75); /* var(--color-secondary) with opacity */
    z-index: 1;
}
.hero-standard-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    margin: 0 auto;
}
.hero-standard .hero-title { /* Use for h1 inside this hero */
    font-family: var(--font-title);
    color: var(--color-white);
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 15px;
    line-height: 1.2;
    text-shadow: 1px 1px 3px rgba(0,0,0,0.4);
}
.hero-standard .hero-subtitle { /* Use for p inside this hero */
    font-family: var(--font-body);
    font-size: 1.15rem;
    line-height: 1.7;
    opacity: 0.9;
    max-width: 700px;
    margin: 0 auto;
}

/* --- Purpose Section --- */
.purpose-section {
    background-color: var(--color-white);
    /* bg-white or bg-light handled by class */
}
.narrow-container { /* For focused text blocks */
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
}
.purpose-section .section-title {
    color: var(--color-secondary); /* If on light bg */
}
.purpose-section .section-title::after {
    background-color: var(--color-secondary); /* If on light bg */
}
.purpose-text {
    font-size: 1.1rem;
    line-height: 1.8;
    color: #444; /* Slightly darker than default paragraph text */
    text-align: center;
    margin-top: -20px; /* Pull up if section title has large bottom margin */
}

/* --- Mission & Vision Section --- */
.mission-vision-section {
    background-color: var(--color-neutral-light); /* Light background */
    /* bg-light or bg-white */
}
.mission-vision-grid {
    display: grid;
    grid-template-columns: 1fr; /* Default to 1 column */
    gap: 40px;
}
.mission-vision-card {
    background-color: var(--color-white);
    padding: 35px;
    border-radius: 8px;
    box-shadow: 0 6px 20px rgba(47, 62, 70, 0.1);
    text-align: left;
}
.mv-icon-container {
    margin-bottom: 20px;
}

.mission-vision-card h3 {
    font-family: var(--font-title);
    font-size: 1.6rem;
    color: var(--color-primary);
    margin-top: 0;
    margin-bottom: 15px;
}
.mission-vision-card p {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--color-secondary);
}

/* --- Advantages Section --- */
.advantages-section {
    /* bg-white or bg-light */
}
.advantages-intro {
    text-align: center;
    max-width: 750px;
    margin: -20px auto 50px auto;
    font-size: 1.05rem;
    color: var(--color-secondary);
}
.advantages-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
}
.advantage-card {
    background-color: var(--color-neutral-light); /* Or var(--color-white) */
    padding: 30px 25px;
    border-radius: 8px;
    text-align: center; /* Align content left within card */
    /* box-shadow: 0 4px 15px rgba(47, 62, 70, 0.07); */
    border: 1px solid #ddd; /* Subtle border */
    display: flex;
    flex-direction: column;
  align-items: center;
}
.advantage-icon {
    width: 100%;
    height: 200px; 
    object-fit: cover; 
    display: block;
    margin-bottom: 10px;
}
.advantage-card h4 {
    font-family: var(--font-title);
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--color-secondary);
    margin-top: 0;
    margin-bottom: 10px;
}
.advantage-card p {
    font-size: 0.9rem;
    line-height: 1.6;
    color: #555;
    flex-grow: 1;
    margin-top: 0;
}

/* --- General Animation Styles (if using JS IntersectionObserver) --- */
.animate-on-scroll {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
}
.animate-on-scroll.visible {
    opacity: 1;
    transform: translateY(0);
}
/* Stagger animations slightly for better effect */
.mission-vision-card.animate-on-scroll:nth-child(2) { transition-delay: 0.2s; }
.advantage-card.animate-on-scroll:nth-child(2) { transition-delay: 0.1s; }
.advantage-card.animate-on-scroll:nth-child(3) { transition-delay: 0.2s; }
.advantage-card.animate-on-scroll:nth-child(4) { transition-delay: 0.3s; }
/* ... and so on, or use AOS data-aos-delay */


/* --- Responsive Adjustments --- */
@media (min-width: 768px) { /* Tablet and up for Mision/Vision */
    .mission-vision-grid {
        grid-template-columns: repeat(2, 1fr); /* Two columns */
    }
    .mission-vision-card {
        text-align: left; /* Align text left in cards */
    }
}

@media (min-width: 992px) { /* Larger screens for Advantages Grid 
    /* Example to force 3 columns if you have 7 items and want the last one centered in a new row */
    /* Or if you use the placeholder and want clean 3 columns */
     .advantages-grid {
        grid-template-columns: repeat(3, 1fr);
    }
    .advantage-card-placeholder {
        display: block;
        background: transparent;
        border: none;
        box-shadow: none;
    } 
    
}

@media (max-width: 767px) { /* Mobile hero */
    .hero-standard { padding: 70px 20px; min-height: 30vh; }
    .hero-standard .hero-title { font-size: 2.2rem; }
    .hero-standard .hero-subtitle { font-size: 1rem; }
    .purpose-text { font-size: 1rem; }
    .mission-vision-card h3 { font-size: 1.4rem; }
    .advantage-card h4 { font-size: 1.1rem; }
    .section-title { /* More space below title */
        line-height: 40px;
        font-size: 1.8rem;
    }
    .section {
        padding: 40px 10px; /* Adjust padding */
    }
}
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
    // ... (Your existing menu toggle script) ...

    // Scroll Animation
    const animatedElements = document.querySelectorAll('.animate-on-scroll');
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
                // observer.unobserve(entry.target); // Optional: stop observing after first animation
            }
        });
    }, { threshold: 0.1 });
    animatedElements.forEach(el => { observer.observe(el); });

    // If using AOS, initialize it:
    // AOS.init({ duration: 700, once: true });
});
</script>